{{- if .Values.posService.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.posService.name }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "restaurant-system.posService.labels" . | nindent 4 }}
spec:
  type: {{ .Values.posService.service.type }}
  ports:
  - port: {{ .Values.posService.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: {{ .Values.posService.name }}
{{- end }}
