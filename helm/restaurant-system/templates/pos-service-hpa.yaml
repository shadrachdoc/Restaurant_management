{{- if and .Values.posService.enabled .Values.posService.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.posService.name }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "restaurant-system.posService.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.posService.name }}
  minReplicas: {{ .Values.posService.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.posService.autoscaling.maxReplicas }}
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.posService.autoscaling.targetCPUUtilizationPercentage }}
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: {{ .Values.posService.autoscaling.targetMemoryUtilizationPercentage }}
{{- end }}
